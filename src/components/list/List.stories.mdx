import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import List from "./List";

<Meta
  title="Design System/List"
  parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# List

### 组件演示

hover to show or hide subMenu

export const data = [
  "item0-mountIndeterminateComponent ",
  "item1-performUnitOfWork",
  "item2-HTMLUnknownElement",
];
export const defaultMenus = (args) => {
  return (
    <List
      data={data}
      renderItem={(item, index) => <List.Item key={index}>{item}</List.Item>}
      {...args}
    ></List>
  );
};

<Story name="List Component">{defaultMenus.bind({})}</Story>

### Usage

```tsx
<List
  data={data}
  renderItem={(item) => <List.Item>{item}</List.Item>}
  {...args}
></List>
```

<ArgsTable of={List} />
